services:
  - type: web
    name: tickr-website
    env: python
    rootDir: tickr_website
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
